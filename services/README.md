# Platform Services

Reserved for signed version manifests, accounts/sessions, update metadata and optional server discovery. Local play and private dedicated-server development should not unnecessarily depend on central services.
