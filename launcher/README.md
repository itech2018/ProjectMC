# ProjectMC Launcher

The launcher is an early core component.

Planned capabilities include side-by-side game versions, installation profiles, stable/beta/alpha/development channels, install/update/uninstall, integrity verification, per-profile settings and saves, game logs, mods/resource packs, backups and server version matching.
